apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: kindmelody
  labels:
    app: kindmelody
    component: postgres
spec:
  type: ClusterIP
  selector:
    app: kindmelody
    component: postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: 5432
      protocol: TCP